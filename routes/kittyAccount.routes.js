const router = require("express").Router();


// POST "api/kitty/create" => Create new account


// GET "api/kitty/all" => get a list of all accounts


// GET "/api/kitty/:id/details" => details of a single account


// PATCH "/api/kitty/:id/edit" => edit name of account


// DELETE "/api/kitty/:id/delete" => delete a specific account (not the one made by the Admin)


module.exports = router;